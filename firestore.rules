rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Helper function to check if user is authenticated
    function isAuthenticated() {
      return request.auth != null;
    }

    // Helper to check ownership for standard collections (allowing legacy documents without ownerId)
    function isOwner(resourceData) {
      return resourceData.get('ownerId', null) == null || resourceData.get('ownerId', null) == request.auth.uid;
    }

    match /transactions/{transactionId} {
      allow read, delete: if isAuthenticated() && isOwner(resource.data);
      allow create: if isAuthenticated() && request.resource.data.ownerId == request.auth.uid;
      allow update: if isAuthenticated() && isOwner(resource.data) && isOwner(request.resource.data);
    }

    match /service_orders/{osId} {
      allow read, delete: if isAuthenticated() && isOwner(resource.data);
      allow create: if isAuthenticated() && request.resource.data.ownerId == request.auth.uid;
      allow update: if isAuthenticated() && isOwner(resource.data) && isOwner(request.resource.data);
    }

    match /backup_history/{backupId} {
      allow read, delete: if isAuthenticated() && isOwner(resource.data);
      allow create: if isAuthenticated() && request.resource.data.ownerId == request.auth.uid;
      allow update: if isAuthenticated() && isOwner(resource.data) && isOwner(request.resource.data);
    }

    match /products/{productId} {
      allow read, delete: if isAuthenticated() && isOwner(resource.data);
      allow create: if isAuthenticated() && request.resource.data.ownerId == request.auth.uid;
      allow update: if isAuthenticated() && isOwner(resource.data) && isOwner(request.resource.data);
    }

    match /staff/{staffId} {
      allow read, delete: if isAuthenticated() && isOwner(resource.data);
      allow create: if isAuthenticated() && request.resource.data.ownerId == request.auth.uid;
      allow update: if isAuthenticated() && isOwner(resource.data) && isOwner(request.resource.data);
    }

    match /appointments/{appointmentId} {
      allow read, delete: if isAuthenticated() && isOwner(resource.data);
      allow create: if isAuthenticated() && request.resource.data.ownerId == request.auth.uid;
      allow update: if isAuthenticated() && isOwner(resource.data) && isOwner(request.resource.data);
    }

    match /equipment_purchases/{purchaseId} {
      allow read, delete: if isAuthenticated() && isOwner(resource.data);
      allow create: if isAuthenticated() && request.resource.data.ownerId == request.auth.uid;
      allow update: if isAuthenticated() && isOwner(resource.data) && isOwner(request.resource.data);
    }

    match /customers/{customerId} {
      allow read, delete: if isAuthenticated() && isOwner(resource.data);
      allow create: if isAuthenticated() && request.resource.data.ownerId == request.auth.uid;
      allow update: if isAuthenticated() && isOwner(resource.data) && isOwner(request.resource.data);
    }

    match /shop_config/{userId} {
      allow read, write: if isAuthenticated() && userId == request.auth.uid;
    }
  }
}
